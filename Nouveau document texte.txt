cd "C:\Program Files\RabbitMQ Server\rabbitmq_server-4.3.0\sbin"
rabbitmq-server.bat


Starting broker... completed with 0 plugins.



cd C:\Users\SAGHI\Documents\restaurant_order_system_final_complete\restaurant_order_system
venv\Scripts\activate
celery -A restaurant_system worker -l info -P solo --without-gossip --without-mingle --without-heartbeat


cd C:\Users\SAGHI\Documents\restaurant_order_system_final_complete\restaurant_order_system
venv\Scripts\activate
python manage.py runserver



cd C:\Users\SAGHI\Documents\restaurant_order_system_final_complete\restaurant_order_system
venv\Scripts\activate
coverage run -m pytest
coverage report



http://127.0.0.1:8000/

http://127.0.0.1:8000/api/docs/


http://127.0.0.1:8000/api/docs/




python manage.py createsuperuser





















أكيد. سأشرح لك المشروع من الصفر كأنك ستبنيه بيدك، خطوة خطوة، من **A إلى Z**، مع ترتيب الملفات والأكواد الأساسية.

سأبدأ من الفكرة العامة، ثم أعطيك تسلسل البناء.

---

# 1. فكرة المشروع

المشروع هو:

**Restaurant Order Management System**

يعني نظام مطعم يسمح بـ:

* تسجيل مستخدم
* تسجيل دخول
* عرض قائمة الطعام
* إضافة منتجات للسلة
* إنشاء طلب
* دفع Online عبر BLIK Simulation
* قبول/رفض الدفع
* دفع Offline يحتاج موافقة Admin
* تشغيل مهام خلفية باستخدام Celery + RabbitMQ
* عرض API عبر Swagger

---

# 2. إنشاء مشروع Django

نبدأ بإنشاء مجلد المشروع:

```cmd
mkdir restaurant_order_system
cd restaurant_order_system
```

إنشاء virtual environment:

```cmd
python -m venv venv
venv\Scripts\activate
```

تثبيت Django والمكتبات:

```cmd
pip install django djangorestframework mysqlclient celery django-celery-results drf-spectacular django-environ pytest pytest-django coverage
```

إنشاء المشروع:

```cmd
django-admin startproject restaurant_system .
```

---

# 3. إنشاء التطبيقات Apps

نقسم المشروع إلى تطبيقات:

```cmd
python manage.py startapp accounts
python manage.py startapp restaurant
python manage.py startapp orders
python manage.py startapp payments
python manage.py startapp frontend
```

معنى كل App:

| App        | وظيفته                          |
| ---------- | ------------------------------- |
| accounts   | تسجيل الدخول والمستخدمين        |
| restaurant | الأصناف، الطاولات، قائمة الطعام |
| orders     | الطلبات وعناصر الطلب            |
| payments   | الدفع Online/Offline            |
| frontend   | صفحات HTML البسيطة              |

---

# 4. تعديل settings.py

داخل:

```text
restaurant_system/settings.py
```

نضيف التطبيقات:

```python
INSTALLED_APPS = [
    ...

    'rest_framework',
    'drf_spectacular',

    'accounts',
    'restaurant',
    'orders',
    'payments',
    'frontend',
]
```

إعداد Swagger:

```python
REST_FRAMEWORK = {
    'DEFAULT_SCHEMA_CLASS': 'drf_spectacular.openapi.AutoSchema',
}

SPECTACULAR_SETTINGS = {
    'TITLE': 'Restaurant Order Management API',
    'DESCRIPTION': 'Django/DRF restaurant ordering system with OAuth2, payments, and async tasks.',
    'VERSION': '1.0.0',
}
```

---

# 5. Models الخاصة بالمطعم

داخل:

```text
restaurant/models.py
```

ننشئ التصنيفات، المنتجات، والطاولات:

```python
from django.db import models


class MenuCategory(models.Model):
    name = models.CharField(max_length=100)
    description = models.TextField(blank=True)

    def __str__(self):
        return self.name


class MenuItem(models.Model):
    category = models.ForeignKey(
        MenuCategory,
        on_delete=models.CASCADE,
        related_name='items'
    )
    name = models.CharField(max_length=150)
    description = models.TextField(blank=True)
    price = models.DecimalField(max_digits=8, decimal_places=2)
    is_available = models.BooleanField(default=True)

    def __str__(self):
        return self.name


class RestaurantTable(models.Model):
    number = models.PositiveIntegerField(unique=True)
    seats = models.PositiveIntegerField(default=2)
    is_active = models.BooleanField(default=True)

    def __str__(self):
        return f'Table {self.number}'
```

---

# 6. Models الخاصة بالطلبات

داخل:

```text
orders/models.py
```

```python
from django.conf import settings
from django.db import models

from restaurant.models import MenuItem, RestaurantTable


class Order(models.Model):
    STATUS_CHOICES = [
        ('PENDING', 'Pending'),
        ('SUBMITTED', 'Submitted'),
        ('REJECTED', 'Rejected'),
        ('PREPARING', 'Preparing'),
        ('READY', 'Ready'),
        ('COMPLETED', 'Completed'),
        ('CANCELLED', 'Cancelled'),
    ]

    customer = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
        related_name='orders'
    )
    table = models.ForeignKey(
        RestaurantTable,
        on_delete=models.SET_NULL,
        null=True,
        blank=True
    )
    status = models.CharField(
        max_length=20,
        choices=STATUS_CHOICES,
        default='PENDING'
    )
    notes = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)

    def total_price(self):
        return sum(item.quantity * item.unit_price for item in self.items.all())

    def __str__(self):
        return f'Order #{self.id}'


class OrderItem(models.Model):
    order = models.ForeignKey(
        Order,
        on_delete=models.CASCADE,
        related_name='items'
    )
    menu_item = models.ForeignKey(
        MenuItem,
        on_delete=models.PROTECT
    )
    quantity = models.PositiveIntegerField(default=1)
    unit_price = models.DecimalField(max_digits=8, decimal_places=2)

    def __str__(self):
        return f'{self.quantity} x {self.menu_item.name}'
```

---

# 7. Models الخاصة بالدفع

داخل:

```text
payments/models.py
```

```python
from django.db import models

from orders.models import Order


class Payment(models.Model):
    METHOD_CHOICES = [
        ('ONLINE', 'Online'),
        ('OFFLINE', 'Offline'),
    ]

    STATUS_CHOICES = [
        ('PROCESSING', 'Processing'),
        ('PENDING', 'Pending'),
        ('APPROVED', 'Approved'),
        ('FAILED', 'Failed'),
        ('REJECTED', 'Rejected'),
    ]

    order = models.ForeignKey(
        Order,
        on_delete=models.CASCADE,
        related_name='payments'
    )
    method = models.CharField(max_length=20, choices=METHOD_CHOICES)
    status = models.CharField(max_length=20, choices=STATUS_CHOICES)
    amount = models.DecimalField(max_digits=8, decimal_places=2)
    transaction_reference = models.CharField(max_length=100, blank=True)
    failure_reason = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)

    def __str__(self):
        return f'Payment #{self.id} - {self.status}'
```

---

# 8. عمل migrations

بعد إنشاء الـmodels:

```cmd
python manage.py makemigrations
python manage.py migrate
```

---

# 9. Admin Panel

داخل:

```text
restaurant/admin.py
```

```python
from django.contrib import admin
from .models import MenuCategory, MenuItem, RestaurantTable

admin.site.register(MenuCategory)
admin.site.register(MenuItem)
admin.site.register(RestaurantTable)
```

داخل:

```text
orders/admin.py
```

```python
from django.contrib import admin
from .models import Order, OrderItem

admin.site.register(Order)
admin.site.register(OrderItem)
```

داخل:

```text
payments/admin.py
```

```python
from django.contrib import admin
from .models import Payment

admin.site.register(Payment)
```

إنشاء admin user:

```cmd
python manage.py createsuperuser
```

---

# 10. Frontend URLs

داخل:

```text
frontend/urls.py
```

```python
from django.urls import path
from . import views

urlpatterns = [
    path('', views.home, name='home'),
    path('menu/', views.menu, name='menu'),
    path('cart/', views.cart, name='cart'),
    path('cart/add/<int:item_id>/', views.add_to_cart, name='add_to_cart'),
    path('cart/remove/<int:item_id>/', views.remove_from_cart, name='remove_from_cart'),
    path('checkout/', views.checkout, name='checkout'),
    path('orders/', views.my_orders, name='my_orders'),
    path('login/', views.customer_login, name='customer_login'),
    path('register/', views.customer_register, name='customer_register'),
    path('logout/', views.customer_logout, name='customer_logout'),
]
```

---

# 11. Main URLs

داخل:

```text
restaurant_system/urls.py
```

```python
from django.contrib import admin
from django.urls import include, path
from drf_spectacular.views import SpectacularAPIView, SpectacularSwaggerView

urlpatterns = [
    path('admin/', admin.site.urls),

    path('', include('frontend.urls')),

    path('api/schema/', SpectacularAPIView.as_view(), name='schema'),
    path(
        'api/docs/',
        SpectacularSwaggerView.as_view(url_name='schema'),
        name='swagger-ui'
    ),
]
```

---

# 12. Frontend views.py

هذا هو قلب الواجهة.

داخل:

```text
frontend/views.py
```

فيه:

* عرض الصفحة الرئيسية
* عرض القائمة
* السلة
* checkout
* login/register/logout
* my orders

الجزء الأهم هو `checkout`.

فكرته:

1. يقرأ cart من session.
2. ينشئ Order.
3. يحسب total.
4. إذا الدفع ONLINE:

   * يأخذ BLIK code.
   * يتحقق أنه 6 أرقام.
   * يحدد هل الدفع ناجح أو فاشل.
   * إذا فشل: Order = REJECTED.
   * إذا نجح: Order = SUBMITTED.
5. إذا الدفع OFFLINE:

   * Payment = PENDING.
   * Admin يوافق لاحقًا.
6. يرسل notification عبر Celery.

الأكواد التي اعتمدناها:

```text
111111 / 222222 / 333333 = APPROVED
999999 / 888888 / 777777 = FAILED
```

---

# 13. Checkout HTML

داخل:

```text
frontend/templates/frontend/checkout.html
```

الصفحة تحتوي:

* اختيار الطاولة
* اختيار طريقة الدفع
* BLIK logo
* إدخال كود BLIK
* countdown overlay

الفكرة:

المستخدم يدخل كود BLIK، ثم يظهر عداد مثل:

```text
10 9 8 7 ...
```

وهذا يحاكي انتظار موافقة البنك أو تطبيق الهاتف.

---

# 14. Static image

ضع شعار BLIK هنا:

```text
frontend/static/images/blik.png
```

وفي HTML نستخدم:

```html
{% load static %}
<img src="{% static 'images/blik.png' %}">
```

---

# 15. Celery setup

داخل:

```text
restaurant_system/celery.py
```

```python
import os
from celery import Celery

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'restaurant_system.settings')

app = Celery('restaurant_system')
app.config_from_object('django.conf:settings', namespace='CELERY')
app.autodiscover_tasks()
```

داخل:

```text
restaurant_system/__init__.py
```

```python
from .celery import app as celery_app

__all__ = ('celery_app',)
```

---

# 16. إعداد RabbitMQ / Celery في settings.py

داخل `settings.py`:

```python
CELERY_BROKER_URL = 'amqp://guest:guest@127.0.0.1:5672//'
CELERY_RESULT_BACKEND = 'rpc://'
```

---

# 17. Payment tasks

داخل:

```text
payments/tasks.py
```

أنشأنا:

```python
send_payment_notification
```

وظيفتها تطبع notification في Celery logs.

وأيضًا أضفنا سابقًا:

```python
process_blik_payment
```

لكن في النسخة الأخيرة جعلنا القرار الأساسي في `checkout` مباشرة حتى تظهر الرسالة الصحيحة فورًا.

---

# 18. تشغيل المشروع

تحتاج 3 نوافذ.

## نافذة 1: RabbitMQ

```cmd
cd "C:\Program Files\RabbitMQ Server\rabbitmq_server-4.3.0\sbin"
rabbitmq-server.bat
```

---

## نافذة 2: Celery

```cmd
cd C:\Users\SAGHI\Documents\restaurant_order_system_final_complete\restaurant_order_system
venv\Scripts\activate
celery -A restaurant_system worker -l info -P solo --without-gossip --without-mingle --without-heartbeat
```

---

## نافذة 3: Django

```cmd
cd C:\Users\SAGHI\Documents\restaurant_order_system_final_complete\restaurant_order_system
venv\Scripts\activate
python manage.py runserver
```

---

# 19. كيف يجري الدفع في مشروعك الآن؟

## Online BLIK success

يدخل المستخدم:

```text
111111
```

النظام يعمل:

```text
Payment = APPROVED
Order = SUBMITTED
```

ويظهر:

```text
Order created successfully
```

---

## Online BLIK failed

يدخل المستخدم:

```text
999999
```

النظام يعمل:

```text
Payment = FAILED
Order = REJECTED
```

ويظهر:

```text
Order rejected because payment failed
```

---

## Offline payment

المستخدم يختار:

```text
Offline admin approval
```

النظام يعمل:

```text
Payment = PENDING
Order = SUBMITTED
```

ثم Admin يوافق أو يرفض لاحقًا.

---

# 20. Swagger

Swagger يعمل من:

```text
http://127.0.0.1:8000/api/docs/
```

وظيفته عرض API endpoints.

---

# 21. GitHub

رفعنا المشروع إلى GitHub، وأضفنا:

* `.gitignore`
* `.env.example`
* README
* GitHub Projects board

---

# 22. GitHub Projects

أنشأنا Sprint Board بأعمدة:

* Todo
* In Progress
* Done

وهذا يغطي Agile/Sprint methodology.

---

# الخلاصة للمبتدئ

المشروع مبني بهذا التسلسل:

```text
Django project
↓
Apps
↓
Models
↓
Database migrations
↓
Admin panel
↓
Frontend pages
↓
Cart system
↓
Checkout logic
↓
BLIK payment simulation
↓
Celery + RabbitMQ
↓
Swagger
↓
Tests
↓
GitHub
```

الفكرة الأساسية:

* Django يدير النظام.
* MySQL يحفظ البيانات.
* Frontend يعرض الصفحات.
* Orders app يدير الطلبات.
* Payments app يدير الدفع.
* Celery + RabbitMQ ينفذان المهام الخلفية.
* Swagger يوثق الـAPI.
* GitHub Projects يثبت العمل بطريقة Sprint.
